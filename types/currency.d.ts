export interface ICurrencyMeta {
    country: string
}

export interface ICurrency {
    name: string
    symbol: string
    description: string
    country: string
    countryId: string
    code: string
}
